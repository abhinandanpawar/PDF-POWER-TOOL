# Quick Wins

1. **Improve Error Handling:** Implement a `GlobalExceptionHandler` to return structured JSON errors instead of default 500 pages.
2. **UI Polish:** Add more animations and hover effects to non-core feature cards.
3. **Expand Tests:** Add unit tests for `PdfMergeService` and `PdfCompressService`.
4. **Fix Warnings:** Resolve deprecation warnings in the build log.
